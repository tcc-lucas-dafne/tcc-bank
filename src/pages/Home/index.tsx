import { Navigate } from "react-router-dom";
import { useAppContext } from "../../context";

const Home = () => {
  const { user } = useAppContext();

  return (
    <div>

    </div>
  )
};

export default Home;